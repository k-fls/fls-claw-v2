id: t2a-dedup-propagation
family: t2-pollloop-keepboth
taxonomy: T2
title: poll-loop.test.ts conflict when the dedup fix first propagates into the module stack
fork_branch: module/interactions-helpers
fork_base_commit: 56e8863b720a33597b4ae5df52cab4fe0bd75eb3
merge_source: 0abdea9438d6bded593b8b1070f0e4803ba9eff4
resolution_ref: 70798713fd0b20c51fafcda0a2a7526ba0419356 # recorded merge with the canonical keep-both resolution (rerere seed)
expected:
  classification: known-recurring
  conflicts:
    - container/agent-runner/src/poll-loop.test.ts
  pois: []
  notes: >
    Replay of recorded merge 70798713fd0b20c51fafcda0a2a7526ba0419356
    ("Merge branch 'module/agent-group-contributions' into
    module/interactions-helpers"); fork_base_commit = its first parent,
    merge_source = its second parent (module/agent-group-contributions after
    the first main_patched propagation, which carries the dedup fix merged at
    3234d29ff54ee9cfe0b42b7e3a3c35782c2b9db1). Canonical resolution: keep both
    sides (dedup machinery + error-classification machinery).
source: recorded fork history, re-verified via merge-tree replay 2026-07-10
replay_notes: >
  Fork-internal propagation merge (the pipeline's propagate step), not an
  upstream range — merge merge_source into the worktree at fork_base_commit.
  At CURRENT branch tips this conflict no longer reproduces (main_patched is
  already propagated), so this historical pin is one of only two
  reproductions. Record the resolution with git rerere to seed
  sweep-state/rr-cache/.
