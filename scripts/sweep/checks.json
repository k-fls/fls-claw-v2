{
  "typecheck": [
    { "cmd": "pnpm run typecheck", "cwd": "." },
    { "cmd": "tsc -p container/agent-runner/tsconfig.json --noEmit", "cwd": "." }
  ],
  "test": [
    { "cmd": "pnpm test", "cwd": "." },
    { "cmd": "bun test", "cwd": "container/agent-runner" }
  ]
}
